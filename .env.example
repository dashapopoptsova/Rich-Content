OPENROUTER_API_KEY=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

# Ошибка SSL: свой PEM (корень прокси), если не помогает truststore + хранилище Windows
# SSL_CERT_FILE=C:\path\to\corp-root.pem

# Принудительно только certifi (без хранилища ОС)
# SSL_USE_CERTIFI=1

# Только для отладки — отключает проверку TLS (небезопасно)
# OPENROUTER_INSECURE_SSL=1
